# API

The live OpenAPI contract is available at `/docs`. Major endpoint groups are `/api/v1/auth`, `/cpses`, `/materials`, `/uploads`, `/matches`, `/national-materials`, `/mappings`, `/dashboard`, and `/health`. Protected calls use a bearer JWT from `POST /api/v1/auth/login`.
