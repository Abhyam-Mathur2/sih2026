# BMIM configuration lives in backend/.env and frontend/.env.
# Copy their respective .env.example files; do not put secrets in this file.
#
# Backend uses Supabase PostgreSQL directly through SQLAlchemy.
# Frontend exposes only VITE_API_BASE_URL.
